[pytest]
asyncio_mode = auto

markers =
    integration: Tests d'intégration nécessitant des services externes (LM Studio, Redis, etc.)

# Exclure les tests d'intégration par défaut en CI :
#   pytest -m "not integration"
# Lancer uniquement les tests d'intégration :
#   pytest -m integration
